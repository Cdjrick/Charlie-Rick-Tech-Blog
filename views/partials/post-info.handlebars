<article class="post">
  <div class="title">
    <a target="_blank">{{title}}</a>
    <p>{{text}}</p>
  </div>
  <div class="meta">
     by {{user.username}} on {{dateFormat created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
  </div>
</article>